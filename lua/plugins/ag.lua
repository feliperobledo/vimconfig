--[[
Wraps the ag (Silver Searcher) CLI so we can run fast project-wide grep commands from inside Neovim.
Remains for legacy keymaps while Telescope-based search is still being modernized, and requires ag to be installed.
]]
return {
  "Numkil/ag.nvim",
}
