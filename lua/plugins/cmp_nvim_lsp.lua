--[[
Provides nvim-cmp with completion items coming from active LSP clients.
Also exposes helper capabilities we pass into lspconfig so servers know completion features are available.
]]
return {
  "hrsh7th/cmp-nvim-lsp",
  opts = {}
}
